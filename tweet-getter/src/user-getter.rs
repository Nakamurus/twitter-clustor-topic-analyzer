use user::{User, Entities, Url};
use tweet::{Tweet, Url};

use dotenv::dotenv;
use serde::Deserialize;
use reqwest::Error;

#[tokio::main]
async fn main() -> Result<(), Error> {
    let request_url = 
}